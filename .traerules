# .traerules — quant_trading_system 工程规则
# 总架构师维护。精简版，以 mcp_bridge.py 实际实现为准。

---

## 一、硬性正确性约束（不可违反）

1. **单调状态机**：订单状态只能前进，终态（FILLED/CANCELLED/REJECTED/EXPIRED）不可被覆盖。所有状态更新必须走统一 CAS 入口。
2. **全链路幂等**：对 `cl_ord_id` + `exec_id` 必须去重。WS 与 REST 可能并发送达同一事件，不得重复记账。
3. **Fail-Closed**：无法确认一致性时，进入 DEGRADED_MODE 并触发 KillSwitch，禁止猜测继续。禁止裸 `except: pass`。
4. **确定性并发**：同一 `cl_ord_id` 的并发处理必须加锁（hashed lock / actor 分区），不依赖时序假设。
5. **Broker 是真相源**：WS 为低延迟驱动，REST 为纠偏；重连后必须先做 REST Alignment 再恢复业务。

---

## 二、架构边界（禁止越界）

| 平面 | 允许 | 禁止 |
|------|------|------|
| **Core** (`core/`) | 领域模型、状态机、纯计算 | 任何 IO、网络、DB、环境变量、sleep |
| **Adapter** (`adapters/`) | WS/REST、限流退避、双驱动合并、对齐恢复 | 直接修改 Core 状态（只能发 canonical events） |
| **Control** (`api/`, `services/`) | 策略注册、风控配置、KillSwitch、审计 | 绕过 Risk/Policy 直接下单 |

---

## 三、KillSwitch 级别

| 级别 | 名称 | 行为 |
|------|------|------|
| L0 | NORMAL | 正常交易 |
| L1 | NO_NEW_POSITIONS | 禁止新开仓，允许平仓/撤单 |
| L2 | CANCEL_ALL_AND_HALT | 撤所有挂单，禁止交易 |
| L3 | LIQUIDATE_AND_DISCONNECT | 强平所有持仓，断开 Broker |

Adapter 进入 DEGRADED_MODE 时，必须通过 Control Plane API 触发全局 KillSwitch L1。

---

## 四、MCP 工作流（双 AI 协作）

### 状态机

```
IDLE
 └─► DEVELOPING          (architect_assign_task)
      └─► REVIEW_PENDING  (engineer_submit_work)
           ├─► APPROVED_FOR_PUSH  (architect_finalize approved=True)
           │    ├─► PR_OPENED     (architect_mark_pr_opened)
           │    │    └─► MERGED   (architect_mark_merged)
           │    └─► MERGED        (architect_mark_merged，直接确认)
           ├─► CLOSED_NO_PR       (architect_finalize，验证型无代码变更)
           └─► REVISE_REQUIRED    (architect_finalize approved=False)
                └─► REVIEW_PENDING (engineer_submit_work，修改后重提)
```

任何已完成状态（MERGED / CLOSED_NO_PR）可重新 `architect_assign_task` 开启新任务。

### 架构师操作（通过 `python mcp_bridge.py` 或 MCP Shell）

| 动作 | 工具函数 | 前置条件 |
|------|----------|----------|
| 发布任务 | `architect_assign_task(task_desc, task_id)` | 工作区干净，Git 健康 |
| 开始正式审核 | `architect_begin_review(note)` | 状态为 REVIEW_PENDING |
| 给出结论 | `architect_finalize(feedback, approved)` | 状态为 REVIEW_PENDING |
| 标记 PR 已创建 | `architect_mark_pr_opened(pr_url, pr_number)` | 状态为 APPROVED_FOR_PUSH |
| 标记已合并 | `architect_mark_merged(merge_commit, summary)` | 状态为 PR_OPENED 或 APPROVED_FOR_PUSH |
| 代执行本地提交 | `architect_commit_for_pr(commit_message)` | 需人类明确授权 |
| 代创建远端 PR | `architect_create_pr(base_branch, title, body)` | 需人类明确授权 |
| 代执行 merge | `architect_merge_pr(pr_number, merge_method)` | 需人类明确授权 |

### 工程师操作（通过 IDE 原生 MCP 工具图标）

| 动作 | 工具函数 |
|------|----------|
| 读取任务指令 | `read_mission_state()` |
| 提交可审快照 | `engineer_submit_work(report, pr_package)` |

### 审查前迭代策略（强制）

- 在架构师 `architect_finalize(approved=true)` 之前，工程师**不得创建 commit**。
- `engineer_submit_work()` 是“审查快照提交”，不是最终代码提交；在未冻结阶段允许反复修改并重复提交。
- 仅当架构师调用 `architect_begin_review()` 进入冻结后，工程师必须停止修改；若需继续修改，先等待打回结论，再重提新快照。
- `pr_description` 与 `report` 必须保持摘要化（短文本）；详细证据放入 `changes` / `test_results` / `spec_alignment`。
- 每次调用 `engineer_submit_work()` 后，必须立即调用 `read_mission_state()`；仅当状态为 `REVIEW_PENDING` 才视为提交成功。
- 若提交响应异常（如 `undefined`）或状态未进入 `REVIEW_PENDING`，按 Fail-Closed 处理：停止后续动作并向架构师报告。

**工程师红线**：
- 严禁手动执行 `python mcp_bridge.py`，必须使用 IDE 原生工具图标。
- 严禁创建临时胶水脚本（`submit_work.py` 等）调用 MCP 工具。
- 收到 `architect_begin_review` 冻结通知后，必须停止修改并回复固定语：`"已停止修改，当前审查版本为最新一次 engineer_submit_work 对应版本。"`

### 控制文件保护

- **绝对禁止**手动编辑 `mcp_mission_control.json`。
- **绝对禁止**修改 `mcp_bridge.py`（基础设施文件，架构师专属）。
- 遇到状态锁定或异常，必须向人类报告，禁止自行"修复"。

### PR 包格式（`engineer_submit_work` 的 `pr_package` 必填字段）

```json
{
  "pr_title": "feat(xxx): 描述",
  "pr_description": "功能说明",
  "changes": [{"file": "path/to/file.py", "type": "modify", "description": "变更说明"}],
  "test_results": {"pytest": "x passed"},
  "risks": ["风险描述"],
  "rollback": "回滚方案"
}
```

---

## 五、Git 操作规则

**允许**：`git status`、`git diff`、`git add`、`git commit`、`git log`

**禁止**：`git push`、`git init`、`git clone`、`git branch`、`git checkout`（分支切换由 MCP 自动处理）

**Fail-Closed**：遇到 `fatal: not a git repository` 或 `detached HEAD`，立即停止并告警，禁止自行修复。

代码推送与 PR 创建默认由人类操作；仅在人类明确授权时，架构师可调用 `architect_commit_for_pr` / `architect_create_pr` / `architect_merge_pr`。

---

## 六、代码规范

- **类型注解**：所有函数签名和类属性必须有类型注解，使用 `str | None` 风格（Python 3.10+）。
- **Async-First**：统一 `asyncio`，禁止在异步上下文使用阻塞网络库（用 aiohttp/httpx async）。锁用 `asyncio.Lock`。
- **格式**：Black（line-length=100）+ isort（black profile）。
- **可观测性**：外部消息必须携带 `local_receive_ts_ms`、`exchange_event_ts_ms`、`seq`、`source`；关键状态迁移打结构化日志含 `trace_id`。
- **输出纪律**：收到编码任务直接输出可运行代码 + 必要注释 + 单测，不复述需求。

---

## 七、AI 集成约束

- AI 只做建议/诊断/分析，不得直接下单或绕过 Risk/Policy/Core。
- 高风险动作（风控阈值变更、KillSwitch 降级）必须 Human-in-the-Loop 确认。
- AI 输出必须结构化、可审计，包含 `trace_id`、输入摘要、输出结果。
- 未通过离线评测/影子运行门禁前，AI 能力不得进入执行链路。
