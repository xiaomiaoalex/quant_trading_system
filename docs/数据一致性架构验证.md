┌─────────────────────────────────────────────────────────────┐
│                    StrategyRunner.tick()                     │
│                                                             │
│  info.tick_count += 1          ← 唯一真相源更新 tick        │
│  info.last_tick_at = now       ← 唯一真相源更新时间         │
│                                                             │
│  if signal and not rate_limited:                            │
│      info.signal_count += 1    ← 唯一真相源更新 signal      │
│      emit "strategy.signal"    ← 事件与计数器同步           │
│                                                             │
│  on TimeoutError / Exception:                               │
│      info.error_count += 1     ← 唯一真相源更新 error       │
│      emit "strategy.error"     ← 事件与计数器同步           │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│            StrategyRuntimeOrchestrator._on_market_data()     │
│                                                             │
│  正常路径: ctx ← sync from info  ← 从真相源同步到 ctx       │
│  异常路径: info.error_count += 1 ← 手动递增（未捕获异常）   │
│            ctx ← sync from info  ← 再同步回 ctx             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    API Routes (strategies.py)                │
│                                                             │
│  RuntimeContextResponse:                                     │
│      tick_count    = info.tick_count    ← 统一从 info 读取  │
│      signal_count  = info.signal_count  ← 统一从 info 读取  │
│      error_count   = info.error_count   ← 统一从 info 读取  │
│      last_tick_at  = info.last_tick_at  ← 统一从 info 读取  │
│      last_error    = info.last_error    ← 统一从 info 读取  │
│      started_at    = info.started_at    ← 统一从 info 读取  │
│      stop_reason   = ctx.stop_reason    ← ctx 独有字段      │
│      primary_symbol= ctx.symbol         ← ctx 独有字段      │
└─────────────────────────────────────────────────────────────┘